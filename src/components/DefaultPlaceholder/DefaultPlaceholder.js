import React from 'react';
import defaultPlaceholder from './defaultPlaceholder.png'
console.log(defaultPlaceholder)

export default function DefaultPlaceholder(){
    return(
        <img src='defaultPlaceholder' alt="placeholder"/>
    )
}